## PR 유형

<!--
[AI 작성 가이드]
- 주 유형은 1개만 선택합니다.
- 보조 유형은 필요하면 복수 선택합니다.
-->

### 주 유형

- [ ] Skill 신규
- [ ] Skill 수정
- [ ] 문서 (docs only)
- [ ] 코드 변경

### 보조 유형

- [ ] 코드 포함
- [ ] 문서 포함
- [ ] 기존 동작 변경 포함

## 배경

<!--
[AI 작성 가이드]
- 반드시 PR 직전까지의 대화/프롬프트를 읽고, 아래를 반영해 작성해 주세요.
  1) 사용자의 원요청(목표)
  2) 중간에 합의된 우선순위/방향
  3) 제약사항(범위, 스타일, 금지사항)
  4) 이번 PR에서 의도적으로 제외한 범위
- "무엇을 바꿨는지"보다 "왜 지금 이 변경이 필요한지"를 먼저 작성합니다.
- 가능하면 이번 PR의 "완료 기준(성공 조건)"을 1~2개로 명시합니다.
- 금지: 모호한 표현("개선", "최적화")만 쓰기.
- 확신이 없는 내용은 단정하지 말고 "가정"으로 표시합니다.
-->

## 구현 사항

<!--
[AI 작성 가이드]
- 구현 나열보다 선택 근거와 의사결정을 중심으로 작성합니다.
- 대안/트레이드오프가 있었다면 왜 제외했는지 포함합니다.
- 핵심 요구사항별로 어떤 구현이 대응되는지 빠짐없이 매핑해 작성합니다.
- 범위를 줄이기 위해 의도적으로 생략한 구현이 있다면 이유를 함께 작성합니다.
-->

### 파일 구조

<!--
[AI 작성 가이드]
- 추가/수정된 파일을 역할별로 그룹핑하여 나열합니다.
- 리뷰어가 어디서부터 읽어야 할지 파악할 수 있도록 작성합니다.

예시 (skill 작업):
```
.claude/skills/{skill-name}/
├── SKILL.md                          — skill 진입점 (트리거, 실행 흐름)
├── {skill-name}-rules.md             — 상세 규칙/프롬프트
└── references/                       — 보조 참조 문서
docs/skills/{skill-name}.md           — 사용법/설계 설명 문서
```

예시 (코드 작업):
```
src/entities/agent/api/http/get-agent.ts   — HTTP 요청 함수
src/entities/agent/api/queries.ts          — React Query 옵션
src/features/agent-create/api/use-create.ts — 커스텀 훅
```
-->

## 검증

<!--
[AI 작성 가이드]
- 실제 수행한 검증만 작성하고, 미실행 항목은 사유를 명시합니다.
- PR 유형에 따라 해당하는 형식을 사용합니다.

[코드 변경]
- 상태가 드러나게 작성합니다: 통과 / 실패 / 미실행.
- 재현성을 위해 검증 환경(브랜치/OS/브라우저 등)을 필요한 범위에서 함께 적습니다.
- 버그 수정이라면 재현 방법 + 수정 확인 방법을 포함합니다.
- 영향 범위(기능/사용자/성능)와 롤백 방안(필요 시)을 포함합니다.

[Skill 변경 — Smoke Test]
- 아래 표를 채워 skill 실행 결과를 구조적으로 기록합니다.
- 주요 시나리오마다 행을 추가합니다.

| 항목 | 내용 |
|---|---|
| 실행 명령 | 예: `/figma-extract https://figma.com/design/...` |
| 입력 조건 | 예: 3프레임, 컴포넌트 2개 포함 |
| 기대 결과 | 예: `docs/specs/login/` 에 spec 3개 생성 |
| 실제 결과 | 예: spec 3개 생성, 검증 통과 |
| 산출물 경로 | 예: `docs/specs/login/base.md` |
| Blocker | 없음 / 있음 (내용 기술) |

[문서만 변경]
- "문서 변경만 포함, 런타임 영향 없음"으로 명시합니다.
-->

## Skill 영향 범위

<!--
[AI 작성 가이드]
- Skill/문서 PR에서만 작성합니다. 코드 전용 PR이면 이 섹션을 삭제합니다.
- 트리거, invocation 조건, description, reference 경로 변경처럼
  "언제 발동되고 무엇을 읽는가"에 영향을 주는 항목을 요약합니다.
- 변경이 없으면 "없음"으로 명시합니다.
-->

### 트리거 변경 사항

<!--
예시:
- description 변경: "..." -> "..."
- 새 트리거 문구 추가: "figma 추출"
- invocation 조건 변경 없음
-->

### 실행 경로 변경 사항

<!--
예시:
- SKILL.md -> references/validation.md 참조 추가
- rules 파일에서 failure-handling reference로 위임
- 신규 reference 파일 2개 추가
-->

## 호환성

<!--
[AI 작성 가이드]
- Skill/문서 PR에서만 작성합니다. 코드 전용 PR이면 이 섹션을 삭제합니다.
- 아래 체크리스트를 확인하고 해당 항목에 x를 표시합니다.
- 영향이 있는 항목은 "없음" 대신 어떤 변화인지 함께 요약합니다.
-->

- [ ] 기존 skill과 트리거 충돌 없음 (동일/유사 트리거를 가진 skill이 없는지 확인)
- [ ] 기존 문서/워크플로와 불일치 없음

### 기존 skill 동작 영향

<!--
[AI 작성 가이드]
- 아래 중 하나를 명시합니다.
  - 없음
  - 있음 (의도된 변경)
  - 있음 (주의 필요)
  - 미확인
- prompt 길이, reference 경로, 실행 순서, 출력 형식 변화가 있으면 적습니다.

예시:
- 있음 (의도된 변경): trigger 충돌 방지를 위해 description 축소
- 있음 (주의 필요): reference 경로 변경으로 기존 문서 링크 업데이트 필요
-->

## 리뷰 포인트

<!--
[AI 작성 가이드]
- 리뷰어에게 확인받고 싶은 질문을 1~3개로 작성합니다.
- 예: "A 대신 B를 선택한 기준이 충분한지?", "이 경계 조건 처리 누락이 없는지?"
- 금지: "리뷰 부탁드립니다"처럼 포괄적 요청만 작성하기.
- 특히 논쟁 가능성이 큰 의사결정 1개를 포함해 집중 리뷰를 요청합니다.
-->

### Reference

<!--
[AI 작성 가이드]
- 대화에서 언급된 참고 링크를 누락 없이 기입합니다.
- 링크가 없으면 "없음"으로 명시합니다.
-->
